# discord-bot-3CK

# Installationseinleitung
1. Führe `pip install -r requirements.txt` aus, um die benötigten Abhängigkeiten zu installieren.
2. Setze den bot token in der Datei `bot.py` ein.
3. Starte den Bot

